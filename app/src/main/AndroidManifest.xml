<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/futureblue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/futureblue"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheFuture">
        <activity android:name=".DiaryActivity">


        </activity>
        <activity android:name=".AddNotesActivity"></activity>
        <activity
            android:name=".SplachActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RamadanMonthActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>